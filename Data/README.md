# Data

The raw CSV is intentionally not included in the repository.

Use the cleaned UTF-8 staging file locally:

`zomato_indore_utf8_clean_staging.csv`

The analytical model uses restaurant name, locality, coordinates, cuisines, average cost for two, rating, votes, online ordering and restaurant type.

Review text is excluded from the core model because of cross-location contamination in the supplied data.
